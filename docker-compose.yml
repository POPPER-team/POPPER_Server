version: '3.8'
services:
#  popper_server:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    ports:
#      - "8080:8080"
#      - "8443:8443"
  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data_container:/data/db    
  minio:
    image: minio/minio
    ports:
      - "9000:9000"  
      - "9001:9001"  
    volumes:
      - minio_storage:/data 
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: Passw0rd123
    command: server --console-address ":9001" /data
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_ROOT_PASSWORD: SQL
    ports:
      - "3306:3306"
  adminer:
    image: adminer
    ports:
      - "8081:8081"
volumes:
  mongodb_data_container:
  minio_storage:  #TODO Define the data volume for MinIO storage